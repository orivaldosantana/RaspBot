cmake_minimum_required(VERSION 2.8)
project( Control )
find_package( OpenCV REQUIRED )
add_executable( control servo_control.cpp )
target_link_libraries( control ${OpenCV_LIBS} -lwiringPi  -lpthread )
